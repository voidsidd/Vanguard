import { FilesIcon, GitMergeIcon, LucideIcon, SearchIcon } from "lucide-react";

export const icon_map: Record<string, LucideIcon> = {
  FilesIcon: FilesIcon,
  SearchIcon: SearchIcon,
  GitMergeIcon: GitMergeIcon,
};
